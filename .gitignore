# dependencies / build
node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# os / editor
.DS_Store

# tooling / local
.claude/
.backups/

# local build/regeneration script (its external source was removed; the app
# runs from the committed app/original-body.html)
map_seo.py
